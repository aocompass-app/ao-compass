# 自動改善をGitHub側で動かす手順

これを入れると、**毎時、勝手にアプリが少しずつ良くなって、承認待ちの提案が届く**ようになります。
届いた提案の「Merge（反映）」を押した瞬間に公開されます。押さなければ何も変わりません。
毎回ファイルをダウンロードして上げ直す作業は、もう要りません。

---

## 1. ファイルを入れる（1回だけ）

同梱のフォルダの中身を、そのままリポジトリに入れます。

```
ao-compass/
├── index.html                        ← 今あるアプリ本体（そのまま）
├── CLAUDE.md                         ← 守るルールとコードの地図
├── package.json                      ← テストを動かす設定
├── .github/
│   └── workflows/
│       └── improve.yml               ← 毎時動かす本体
├── .claude/
│   ├── commands/improve.md           ← 改善のやり方
│   └── improve-state.json            ← 毎時か1日1回かの状態
└── tests/
    └── smoke.mjs                     ← 壊れていないか確かめるテスト
```

https://github.com/aocompass-app/ao-compass/upload/main を開いて、
**フォルダごとドラッグ**してください。`.github/workflows/` の階層のまま入ります。
最後に下の「Commit changes」を押します。

---

## 2. Claude をリポジトリに入れる（1回だけ）

https://github.com/apps/claude を開いて Install。
`aocompass-app/ao-compass` だけを選んでください。

---

## 3. APIキーを登録する（1回だけ）

https://console.anthropic.com でキーを作ります（`sk-ant-...` で始まる文字列）。

そのあと
https://github.com/aocompass-app/ao-compass/settings/secrets/actions
→ 「New repository secret」

- Name: `ANTHROPIC_API_KEY`
- Secret: さきほどのキーを貼り付け

**このキーはチャットに貼らないでください。** GitHub の画面に直接入れるだけです。
GitHub 側でも隠されるので、あとから中身を見ることはできません。

---

## これで終わりです

毎時51分に自動で動きます。すぐ試したいときは
https://github.com/aocompass-app/ao-compass/actions
→ 「AO Compass 自動改善」→ 「Run workflow」。

動いたあとは
https://github.com/aocompass-app/ao-compass/pulls
に提案が並びます。中身を読んで、良ければ「Merge pull request」。
だめなら「Close」で捨てるだけです。**main には勝手に入りません。**

案が出尽くしてきたら、その回の判断で自動的に「1日1回」に落ちます。
頻度が勝手に上がることはありません。

---

## 費用について

- GitHub Actions：公開リポジトリなら無料枠で足ります。
- APIの利用料：1回あたり数十円程度が目安です。毎時だと1日で数百円ほど。
  高いと感じたら `.claude/improve-state.json` の `"hourly"` を `"daily"` に書き換えれば
  1日1回になります。
- 使いすぎが心配なら、Anthropic の管理画面で月ごとの上限を設定できます。

---

## 手元のパソコンで動かしたいとき

リポジトリを取ってきて Claude Code を開き、`/improve` と打つだけです。
GitHub の設定をしなくても、同じ改善の流れが手元で動きます。

```bash
git clone https://github.com/aocompass-app/ao-compass
cd ao-compass
npm i && npx playwright install chromium
```

---

アプリ https://aocompass-app.github.io/ao-compass/
アップロード https://github.com/aocompass-app/ao-compass/upload/main
